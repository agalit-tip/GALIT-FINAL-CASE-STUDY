---
  - name: CHECK IP ADDRESSES
    hosts: routers
    gather_facts: false
    connection: local
    vars:
      cli:
        username: cisco
        password: cisco123!
    tasks:
      - name: PRINT IP INTERFACES
        ios_command:
          provider: "{{ cli }}"
          commands:
            - show ip interface brief
        register: config
      - name: SAVE OUTPUT TO ./iosconfig/
        copy:
          content: "{{config.stdout[0]}}"
          dest: "iosconfig/showip{{inventory_hostname}}.txt"